# CTD-Capstone2
Code The Dream Capstone Project number 2

scraper.py:
    Scraped the website, then turned into a csv

db.py:
    read the csv then turned it into a dataframe
    cleaned the temperatures to make into a float instead of an object (if needed calculations)
    connected cleaned dataframe into a database